{
  "name": "figure-it-out",
  "version": "0.1.0",
  "private": true,
  "description": "Figure It Out community figure drawing group website",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
