# Figure It Out

Static site for the Figure It Out community figure drawing group, built with Eleventy.

## Getting started

### Develop

```sh
npm run dev
```

### Build

```sh
npm run build
```

> Note: This project uses `npx` to run Eleventy, so no dependency install is required.
